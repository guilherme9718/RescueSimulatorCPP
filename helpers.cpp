#include <vector>
using namespace std;

class Helpers {
    private:
        Helpers() {}
    public:
        template <typename T>
        void ImprimirVetor(vector<T> vetor) {
            //for(int i=0; i < vetor.)
        }
};